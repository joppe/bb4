/*global define*/

define(['jquery', 'backbone'], function ($, Backbone) {
    'use strict';

    var Team;

    Team = Backbone.Model.extend({
        defaults: {

        }
    });

    return Team;
});