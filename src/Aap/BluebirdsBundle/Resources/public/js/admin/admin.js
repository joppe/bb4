/*global jQuery, _, Backbone*/

jQuery(function ($) {
    'use strict';

    /**
     * the router defines routes that point to a page, so a route instantiates a page
     *
     * there are the following type of pages:
     * - list
     * - detail
     * - form (will mostly be displayed in a lightbox/modal)
     */



});